export const DEFAULT_STATIC_IMAGES = [
  'default-avatar.jpg',
];

export const STATIC_RESOURCE_FIELDS = [
  'avatar',
  'previewImage',
  'images'
];

export enum RestRoute {
  USERS = '/users',
  OFFERS = '/offers',
  COMMENTS = '/comments',
  UPLOAD = '/upload',
  STATIC = '/static',
}
